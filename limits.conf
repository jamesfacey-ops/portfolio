# limits.conf — http context (auto-included by nginx:alpine via /etc/nginx/conf.d/*.conf)
# MUST be a separate file — limit_req_zone is an http-context directive,
# it cannot be defined inside a server {} block.
limit_req_zone $binary_remote_addr zone=contact:1m rate=3r/m;
